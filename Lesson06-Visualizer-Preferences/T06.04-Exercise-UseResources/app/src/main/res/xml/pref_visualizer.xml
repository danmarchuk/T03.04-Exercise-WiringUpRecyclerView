<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- TODO (2) Create a res->bools.xml file and create a boolean for this default value -->
    <!-- TODO (3) Update these values to be values from resource files and not hard coded -->

    <CheckBoxPreference
        android:defaultValue="@bool/shared_prefs_bool"
        android:key="@string/checkbox_prefs"
        android:summaryOff="@string/summary_off_string"
        android:summaryOn="@string/summary_on_string"
        android:title="@string/checkbox_prefs_title" />

</PreferenceScreen>
